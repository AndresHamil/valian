export type ProcessViewCopy = {
  fallbackTitle: string;
  fallbackDescription: string;
  body: string;
};

export type ProtectedProcessState = {
  permiso: boolean;
  titulo: string;
  descripcion: string;
};